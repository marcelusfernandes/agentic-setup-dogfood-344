# agentic-dogfood-adopt-20260919

A disposable repository created to run `agentic-setup`'s `adopt` sequence end to end
once, against a project nobody prepared (issue #344).

It is the smallest honest project: one dependency-free test, run by `npm test`.
